Q-1. Write a C program to check whether a given element is present in an array of elements using linear search. The program should prompt the user to enter the size of the array, the elements of the array, and the element to search for?
Sol-1 
#include<stdio.h>
int main(){
	int i,n,key,a[100],flag=0,pos;
	scanf("%d",&n);
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
	}
	scanf("%d",&key);
	for(i=0;i<n;i++){
		if(a[i]==key){
			pos=i;
			flag=1;
			break;
		}
	}
	if(flag==1){
		printf("found at position %d\n",pos);
	}else{
		printf("%d not found\n",key);
	}
}

Q-2. Write a C program that performs a binary search to find the position of the given integer in the array?
Sol-2 
#include <stdio.h>
int binarySearch(int arr[], int n, int x) {
    int left = 0, right = n - 1;
    while (left <= right) {
        int mid=left+(right-left)/2;
		if(arr[mid]==x){
			return mid;
		}
		else if(x<arr[mid]){
			right=mid-1;
		}
		else{
			left=mid+1;
		}
    }
    return -1;
}

int main() {
    int n, x;
    
    scanf("%d", &n);
    
    int arr[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    
    scanf("%d", &x);
    
    int result = binarySearch(arr, n, x);
    
    if (result != -1)
        printf("Element found at index %d\n", result);
    else
        printf("Element not found\n");

    return 0;
}
